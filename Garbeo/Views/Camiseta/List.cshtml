@model CamisetaListViewModel


    <div class="py-5 bg-white">
        <div class="container">
            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
                @foreach (var camiseta in Model.Camisetas)
                {
                   <partial name="_CamisetaCard" model="camiseta"/>
                }
            </div>
        </div>
    </div>
